// import { Test, TestingModule } from '@nestjs/testing';
// import { Pay } from './pay';

describe('Pay', () => {
  // let provider: Pay;
  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     providers: [Pay],
  //   }).compile();
  //   provider = module.get<Pay>(Pay);
  // });
  // it('should be defined', () => {
  //   expect(provider).toBeDefined();
  // });
});
